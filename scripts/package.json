{
  "name": "claude-scripts",
  "private": true,
  "type": "module",
  "dependencies": {
    "@linear/sdk": "^78.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "tsx": "^4.21.0"
  },
  "scripts": {
    "fetch-reviews": "npm --prefix reviews run fetch-reviews --",
    "check-reviews": "npm --prefix reviews run check-reviews --",
    "test:reviews": "npm --prefix reviews test"
  }
}
