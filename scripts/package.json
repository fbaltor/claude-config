{
  "name": "claude-scripts",
  "private": true,
  "type": "module",
  "dependencies": {
    "@linear/sdk": "^78.0.0",
    "beautiful-mermaid": "^1.1.3"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  },
  "scripts": {
    "fetch-reviews": "pnpm --dir reviews run fetch-reviews --",
    "check-reviews": "pnpm --dir reviews run check-reviews --",
    "test": "node --import tsx --test __tests__/*.test.ts",
    "test:reviews": "pnpm --dir reviews test",
    "test:meta-workflow": "pnpm --dir meta-workflow test"
  }
}
